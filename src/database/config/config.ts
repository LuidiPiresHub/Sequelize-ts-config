import { Options } from "sequelize";

const config: Options = {
  username: 'root',
  password: 'password',
  database: 'Estudos2',
  host: 'localhost',
  port: 3306,
  dialect: 'mysql',
}

export = config;
